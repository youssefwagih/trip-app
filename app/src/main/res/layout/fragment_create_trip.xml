<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.create_trip.CreateTripFragment">

    <EditText
        android:id="@+id/maxPassengerCountEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/padding_small"
        app:layout_constraintTop_toTopOf="parent"
        android:ems="10"
        android:inputType="number"
        android:hint="@string/max_passengers_count" />

    <EditText
        android:id="@+id/tripCostEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/padding_small"
        app:layout_constraintTop_toBottomOf="@+id/maxPassengerCountEditText"
        android:ems="10"
        android:inputType="number"
        android:hint="@string/trip_cost" />

    <Button
        android:id="@+id/createTripButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/create_trip"
        app:layout_constraintBottom_toBottomOf="parent"/>

</android.support.constraint.ConstraintLayout>